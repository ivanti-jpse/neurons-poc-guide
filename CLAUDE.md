# Ivanti Neurons POCガイド 執筆ルール

このドキュメントは、POCガイドサイト(MkDocs Material)にページを追加する際の標準ルールです。
新しいガイドページ・検証項目を作成する際は、必ずこのルールと `agent-install.md` / `checklist/agent-install.md` の実例を参照してください。

---

## 1. 情報源のルール

- 参照する Ivanti ヘルプは **必ず日本語版(`ja_JP`)** を使う。URLパターン: `https://help.ivanti.com/ht/help/ja_JP/CLOUD/vNow/xxx.htm`
- 理由: コンソール UI が日本語表示のため、英語版ヘルプの表記と実際の画面表記が一致しない
- 日本語版に存在しないページ・未翻訳の項目は、英語版を参照した上で **その旨を明記**する(下記「UI表記の確認ルール」参照)

## 2. UI表記の確認ルール(最重要)

- コンソールの実際のメニュー名・ボタン名を本文に書く際は `[エージェント] > [エージェント配布]` のように **角カッコで統一表記**する
- **ヘルプ記載の文言と、Claude in Chrome で実機コンソールを確認した文言が一致した場合のみ確定表記として記載する**
- 実機確認できず、ヘルプの英語表記から推定訳した場合は、該当箇所に必ず以下の admonition を添える:

  ```markdown
  !!! warning "UI 表記の確認"
      この表記は日本語版ヘルプで確認できていない暫定訳です。
      実際のテナント画面の表示に合わせて修正してください。
  ```

- Claude in Chrome でコンソールにアクセスできる場合は、**ガイド化の最終ステップとして実機のメニュー名・ボタン名・画面遷移を必ず確認**し、暫定訳を確定表記に置き換える

## 2.5 フォルダ・ファイル命名規則

- ガイド本文: `docs/<カテゴリ>/<機能名>.md`(例: `docs/neurons/agent-install.md`)
- 検証項目: `docs/checklist/<機能名>.md`(**カテゴリ名は含めず、機能名のみ**。例: `docs/checklist/agent-install.md`)
- `<機能名>` は両者で完全に一致させる(ガイドと検証項目をファイル名で対応付けられるようにするため)
- ガイドと検証項目を同一フォルダにネストする構成(例: `docs/agent-install/checklist/agent-install.md`)は **採用しない**。理由: 「設定手順」「POC検証項目」という2つの独立したnavタブに対応させるには、フォルダを分けた方が `mkdocs.yml` の記述・将来の一括処理(Excel生成等)の両方でシンプルになるため

## 2.6 前提条件(Prerequisites)ファイルの構成ルール

各種「要件」に関するドキュメントは、機能ガイド(`docs/<カテゴリ>/`)や検証項目(`docs/checklist/`)とは別に、
**`docs/prerequisites/` フォルダに集約**する。

### 対象ファイルと配置

| ファイル | 内容 |
| --- | --- |
| `docs/prerequisites/network-requirements.md` | ネットワーク要件(通信先、ポート、プロキシ要件など) |
| `docs/prerequisites/platform-requirements.md` | プラットフォーム要件(対応OS、バージョン、ライセンス要件など) |
| `docs/prerequisites/patch-requirements.md` | パッチ要件(パッチ管理機能に関する前提条件) |
| `docs/prerequisites/browser-requirements.md` | ブラウザ要件(管理コンソールの対応ブラウザなど) |

### 配置ルール

- 上記4ファイルは **機能ガイド本文からは分離**し、各ガイド本文からは該当する要件ファイルへリンクする形にする
  (例: `agent-install.md` の「前提条件」セクションから `../prerequisites/network-requirements.md` を参照するなど)
- 新しい前提条件のカテゴリが今後必要になった場合も、同様に `docs/prerequisites/<種別>-requirements.md` の命名パターンに従う
- 既存で `docs/` 直下に置かれていた `network-requirements.md` は `docs/prerequisites/network-requirements.md` へ移動済み

### nav への反映

`mkdocs.yml` の `nav:` トップレベルは **ホーム / 前提条件 / 設定手順 / POC 検証項目** の4タブ構成とする。
「前提条件」は「設定手順」にネストせず、**独立したトップレベルタブ**として配置する:

```yaml
nav:
  - ホーム: index.md
  - 前提条件:
      - ネットワーク要件: prerequisites/network-requirements.md
      - プラットフォーム要件: prerequisites/platform-requirements.md
      - パッチ要件: prerequisites/patch-requirements.md
      - ブラウザ要件: prerequisites/browser-requirements.md
  - 設定手順:
      - エージェントのインストール: neurons/agent-install.md
  - POC 検証項目:
      - エージェントのインストール: checklist/agent-install.md
```

(検証項目タブには前提条件用のチェック表は作らない。前提条件は「満たしているかを事前確認するもの」であり、
POC実施後の検証結果一覧とは性質が異なるため)

### 前提条件ページの構成テンプレート

前提条件ページは、機能ガイドページの完全なテンプレート(3章)を適用せず、以下の簡易構成でよい:

1. H1タイトル(例: `# ネットワーク要件`)
2. 概要(1〜2行)
3. 要件を表形式で整理(項目 / 要件内容 / 備考)
4. 参照元 admonition(該当する日本語版ヘルプへのリンク)

## 3. ページ構成のテンプレート

新規ガイドページ(`docs/<カテゴリ>/<機能名>.md`)は、この順序で構成する:

1. **H1タイトル**(例: `# Neurons Agent のインストール`)+ 1〜2行の概要
2. **参照元 admonition**(`!!! info "参照元"`)— 参照した日本語版ヘルプへのリンクを箇条書きで列挙
3. **前提条件**(H2)— 事前に満たすべき条件。OS要件、権限、ネットワーク要件など
4. **全体ワークフロー**(H2、必要な場合)— 複数ステップに分かれる操作は mermaid の `graph LR` で図示してから、H3で各ステップを解説
5. **手順本体**(H2)— OSやエディションで手順が分岐する場合は `=== "Windows"` 形式のタブ切り替えを使う
6. **手順後の確認**(H2)— 正常に完了したことを確認する方法(コンソール側/エンドポイント側の両方があれば分けて記載)
7. **トラブルシューティングの入口**(H2)— 表形式で「症状 / 最初に確認すること」を2〜5行程度、深入りしない
8. **末尾に POC 検証項目タブへの誘導**(下記「4. 検証項目の追加ルール」参照)

見出しレベルは H1(タイトルのみ)→ H2(大項目)→ H3(小項目)を厳守し、飛び番(H1の次にH3等)を作らない。

## 4. 検証項目の追加ルール

- チェック表は **ガイド本文には書かず**、必ず `docs/checklist/<機能名>.md` に分離する
- 列は **# / カテゴリ / 検証項目 / 期待結果** の4列のみ(結果記入欄・確認日・備考などは付けない — これは「見るための一覧」であり「記入するための表」ではない)
- カテゴリは時系列で並べる(例: 事前準備 → インストール/設定 → 登録確認 → コンソール確認)
- 検証項目の文末は「〜できるか」で統一(実施記録ではなく参照用のため)
- ガイド本文の末尾には本文中に表を書かず、以下の1文とリンクのみを置く:

  ```markdown
  !!! abstract "POC 検証項目"
      本ページに対応する検証項目の一覧は、[POC 検証項目](../checklist/<機能名>.md) タブを参照してください。
  ```

- `mkdocs.yml` の `nav:` に、対応する2エントリを追加する(「設定手順」タブ側と「POC 検証項目」タブ側)

## 5. デザイン・技術仕様(変更しない)

- テーマ: MkDocs Material、`language: ja`
- ブランドカラー: `docs/stylesheets/extra.css` の Ivanti レッド(`--ivanti-red: #c8102e`)を継承。**正式なブランドガイドラインのカラーコードが判明するまでは変更しない**
- 拡張機能: `admonition` / `pymdownx.tabbed`(OS別タブ) / `pymdownx.superfences`(mermaid図含む) / `attr_list` / `md_in_html`
- 検索: `search` プラグイン、`lang: ja`
- 配布用ビルド設定(`mkdocs-offline.yml` / `mkdocs-singlefile.yml`)は共通設定を `INHERIT` しているため、ページ追加時に個別対応は不要

## 6. 作業の進め方(1トピックあたり)

1. 対象機能の日本語版ヘルプを読み込み、内容を理解する
2. Claude in Chrome で管理コンソールにアクセスし、該当機能の実際の操作画面・メニュー名・手順を確認する
3. 上記「3. ページ構成のテンプレート」に沿ってガイド本文(`docs/<カテゴリ>/<機能名>.md`)を作成する
4. 「4. 検証項目の追加ルール」に沿って検証項目ページ(`docs/checklist/<機能名>.md`)を作成する
5. `mkdocs.yml` の `nav:` を更新する
6. `mkdocs build` でエラー・警告(特にリンク切れ)がないことを確認する
7. 暫定訳が残っている箇所を洗い出し、ユーザーに確認を依頼する

## 7. やらないこと

- ガイド本文へのチェックボックス・進捗記入欄の実装(別途動的な仕組みを検討中のため、現時点では静的な参照一覧に留める)
- レイアウト・配色・ナビゲーション構造の変更(要望があるまで現状維持)
- 英語版ヘルプの文言をそのまま日本語ページに残すこと(必ず翻訳し、未確認なら暫定訳と明記)
